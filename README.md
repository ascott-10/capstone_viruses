# Capstone Project

This project loads raw mutant and wildtype images, processes them, and (later) builds models to classify/analyze them.

## Structure

- `code/`: Code for loading data and modeling
- `data/`: Raw image data
- `notebooks/`: Jupyter notebooks
- `main.py`: Main runner script

## Setup

Install requirements:

